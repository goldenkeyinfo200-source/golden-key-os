{
  "name": "golden-key-os",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["backend", "crm", "telegram-bot", "shared"],
  "scripts": {
    "dev:backend": "npm --workspace backend run dev",
    "dev:crm": "npm --workspace crm run dev",
    "dev:bot": "npm --workspace telegram-bot run dev",
    "build": "npm run build --workspaces --if-present"
  }
}
